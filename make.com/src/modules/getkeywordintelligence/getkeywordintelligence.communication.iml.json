{
    "url": "/rootscan/keyword-intelligence?correlationId={{parameters.correlationId}}",
    "method": "POST",
    "body": {
        "rootScanRequest": {
            "clientUrl": "{{parameters.clientUrl}}",
            "industry": "{{parameters.industry}}",
            "services": "{{parameters.services}}",
            "competitors": [],
            "submissionId": "make-module-{{parameters.correlationId}}",
            "clientInfo": {
                "companyName": "{{parameters.companyName}}",
                "email": "{{parameters.email}}"
            }
        }
    }
}